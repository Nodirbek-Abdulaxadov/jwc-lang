class Counter {
    let value = 0;

    function inc() {
        this.value = this.value + 1;
        return this.value;
    }
}

function main() {
    let c = new Counter();
    print(c.inc());
    print(c.inc());
    print(c.value);
}
