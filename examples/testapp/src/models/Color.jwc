entity ColorEntity of AppDbContext {
    id   int pk;
    name varchar(255);
    hex  varchar(7);
}

class ColorCreateRequest {
    id int;
    name string;
    hex string;
}

class ColorUpdateRequest {
    name string;
    hex string;
}
