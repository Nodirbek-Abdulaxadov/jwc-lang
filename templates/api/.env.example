# Copy to `.env` and fill in. `jwc serve` reads these.
DATABASE_URL=postgres://postgres@localhost/{{name}}
PORT=8080
DB_POOL=20
# Signs the keyset-pagination cursors. Any long random string.
CURSOR_SECRET=change-me
