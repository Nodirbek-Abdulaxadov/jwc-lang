entity User of AppDb {
    id           int pk autoincrement;
    email        varchar(120);
    passwordHash varchar(255);
    createdAt    datetime;
}

class RegisterRequest {
    email    string;
    password string;
}

class LoginRequest {
    email    string;
    password string;
}
