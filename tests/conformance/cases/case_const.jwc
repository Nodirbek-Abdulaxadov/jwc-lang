// Conformance: const declarations are evaluated at module scope.
const PI = 3;
const TAU = PI * 2;
const NAME = "jwc";
const FLAG = true;

function main() {
    print(PI);
    print(TAU);
    print(NAME);
    print(FLAG);
    print(NAME + "-v" + TAU);
}
