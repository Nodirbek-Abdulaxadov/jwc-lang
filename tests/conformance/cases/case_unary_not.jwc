// Conformance: `!expr` unary boolean negation.
function main() {
    print(!true);
    print(!false);
    let ok = true;
    if (!ok) {
        print("hit");
    } else {
        print("skip");
    }
    let dead = false;
    if (!dead) {
        print("alive");
    }
}
