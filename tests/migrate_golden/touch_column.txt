══ 0001_touch_column.up.sql
-- Generated by `jwc migrate new`. Do not edit.
-- 0001_touch_column
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 2 · tables and columns ──
ALTER TABLE org.orgs ADD COLUMN updated_at timestamptz DEFAULT now() NOT NULL;

-- ── 3 · data ──
-- jwc:data 0001_touch_column
-- A `0001_touch_column.data.sql` sidecar, if present, is applied here
-- (migrations.md §7.2). It is hand-written and never generated.

-- ── 6 · functions and triggers ──
CREATE OR REPLACE FUNCTION org.tg_orgs__touch()
RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN
  NEW.updated_at := now();
  RETURN NEW;
END $$;
CREATE TRIGGER tg_orgs__touch
    BEFORE UPDATE ON org.orgs
    FOR EACH ROW EXECUTE FUNCTION org.tg_orgs__touch();

COMMIT;

══ 0001_touch_column.down.sql
-- Generated by `jwc migrate new`. Do not edit.
-- down of 0001_touch_column
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 9 · destructive ──
DROP TRIGGER IF EXISTS tg_orgs__touch ON org.orgs;
ALTER TABLE org.orgs DROP COLUMN updated_at;
DROP FUNCTION IF EXISTS org.tg_orgs__touch();

COMMIT;
