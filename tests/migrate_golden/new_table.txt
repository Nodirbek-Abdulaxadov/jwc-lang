══ 0001_new_table.up.sql
-- Generated by `jwc migrate new`. Do not edit.
-- 0001_new_table
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 2 · tables and columns ──
CREATE TABLE org.notes (
    id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    org_id bigint NOT NULL,
    body text NOT NULL,
    CONSTRAINT pk_notes PRIMARY KEY (id)
);

-- ── 3 · data ──
-- jwc:data 0001_new_table
-- A `0001_new_table.data.sql` sidecar, if present, is applied here
-- (migrations.md §7.2). It is hand-written and never generated.

-- ── 4 · constraints ──
ALTER TABLE org.notes ADD CONSTRAINT fk_notes__org_id
    FOREIGN KEY (org_id) REFERENCES org.orgs (id)
    ON DELETE CASCADE;

-- ── 5 · indexes ──
CREATE INDEX ix_notes__org_id
    ON org.notes (org_id);

-- ── 7 · comments ──
COMMENT ON TABLE org.notes IS 'Free-form notes on an org.';
COMMENT ON COLUMN org.notes.body IS 'Markdown.';

COMMIT;

══ 0001_new_table.down.sql
-- Generated by `jwc migrate new`. Do not edit.
-- down of 0001_new_table
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 9 · destructive ──
DROP TABLE org.notes;

COMMIT;
