══ 0001_view_body.up.sql
-- Generated by `jwc migrate new`. Do not edit.
-- 0001_view_body
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 0 · drop view ──
DROP VIEW org.org_summary;

-- ── 3 · data ──
-- jwc:data 0001_view_body
-- A `0001_view_body.data.sql` sidecar, if present, is applied here
-- (migrations.md §7.2). It is hand-written and never generated.

-- ── 8 · create view ──
CREATE VIEW org.org_summary AS
SELECT t0.id AS id,
       t0.slug AS slug,
       t0.name AS name
  FROM org.orgs t0;
COMMENT ON VIEW org.org_summary IS 'Orgs, flattened.';

COMMIT;

══ 0001_view_body.down.sql
-- Generated by `jwc migrate new`. Do not edit.
-- down of 0001_view_body
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 0 · drop view ──
DROP VIEW org.org_summary;

-- ── 8 · create view ──
CREATE VIEW org.org_summary AS
SELECT t0.id AS id,
       t0.slug AS slug
  FROM org.orgs t0;
COMMENT ON VIEW org.org_summary IS 'Orgs, flattened.';

COMMIT;
