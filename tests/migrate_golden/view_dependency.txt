══ 0001_view_dependency.up.sql
-- Generated by `jwc migrate new`. Do not edit.
-- 0001_view_dependency
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 0 · drop view ──
DROP VIEW org.org_summary;

-- ── 2 · tables and columns ──
ALTER TABLE org.orgs ALTER COLUMN name TYPE varchar(200) USING name::varchar(200);

-- ── 3 · data ──
-- jwc:data 0001_view_dependency
-- A `0001_view_dependency.data.sql` sidecar, if present, is applied here
-- (migrations.md §7.2). It is hand-written and never generated.

-- ── 8 · create view ──
CREATE VIEW org.org_summary AS
SELECT t0.id AS id,
       t0.slug AS slug
  FROM org.orgs t0;
COMMENT ON VIEW org.org_summary IS 'Orgs, flattened.';

COMMIT;

══ 0001_view_dependency.down.sql
-- Generated by `jwc migrate new`. Do not edit.
-- down of 0001_view_dependency
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 0 · drop view ──
DROP VIEW org.org_summary;

-- ── 2 · tables and columns ──
ALTER TABLE org.orgs ALTER COLUMN name TYPE varchar(80) USING name::varchar(80);

-- ── 8 · create view ──
CREATE VIEW org.org_summary AS
SELECT t0.id AS id,
       t0.slug AS slug
  FROM org.orgs t0;
COMMENT ON VIEW org.org_summary IS 'Orgs, flattened.';

COMMIT;
