══ 0001_rename_table.up.sql
-- Generated by `jwc migrate new`. Do not edit.
-- 0001_rename_table
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 2 · tables and columns ──
ALTER TABLE org.orgs RENAME TO tenants;

-- ── 3 · data ──
-- jwc:data 0001_rename_table
-- A `0001_rename_table.data.sql` sidecar, if present, is applied here
-- (migrations.md §7.2). It is hand-written and never generated.

-- ── 4 · constraints ──
ALTER TABLE org.tenants RENAME CONSTRAINT pk_orgs TO pk_tenants;
ALTER TABLE org.tenants RENAME CONSTRAINT uq_orgs__slug TO uq_tenants__slug;

COMMIT;

══ 0001_rename_table.down.sql
-- Generated by `jwc migrate new`. Do not edit.
-- down of 0001_rename_table
--
-- Phases (migrations.md §4): drop view, schemas and types, tables and
-- columns, data, constraints, indexes, functions and triggers, comments,
-- create view, destructive.

BEGIN;

-- ── 2 · tables and columns ──
ALTER TABLE org.tenants RENAME TO orgs;

-- ── 4 · constraints ──
ALTER TABLE org.orgs RENAME CONSTRAINT pk_tenants TO pk_orgs;
ALTER TABLE org.orgs RENAME CONSTRAINT uq_tenants__slug TO uq_orgs__slug;

COMMIT;
