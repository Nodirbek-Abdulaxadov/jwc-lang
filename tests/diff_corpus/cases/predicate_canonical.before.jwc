namespace c;
database App : Postgres;
schema org of App;

table Orgs of App.org {
    id         bigint primary key identity;
    slug       varchar(40);
    retired_at timestamptz?;

    unique (slug) where retired_at == null and id > 0 : "faol slug bitta";
}
